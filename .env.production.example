# 环境变量已按部署场景拆分，请复制对应文件为 .env：
#
#   cp .env.default .env   # 源仓根路径（build.sh，【必改】0 项）
#   cp .env.example .env   # Fork /mmw/ 子路径（build-fork.sh，默认【必改】0 项）
#
# 说明见 .env.default / .env.example 顶部注释；迭代文档见 docs/iteration/basepath-extension.md
